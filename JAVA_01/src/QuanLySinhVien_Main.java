/**
 * QuanLySinhVien_Main
 */
public class QuanLySinhVien_Main {
    Student_manager manager = new Student_manager();manager.InputList(Student_manager.list);manager.OutputList(Student_manager.list);

    public void sapxeptheoten(Student list[])
    {
        if(list[i].getName().compataeto(list[j].getname())>0) // Sap Xep theo ten
    }
}